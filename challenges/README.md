# CHALLENGES

In order to prove this project works as intended we will need to pass certain challenges defined in this folder. 

The holy grail is `SELF_COMPILATION.md` which means the `intent` files in `intentc` are sufficiently well specified such that they can recompile the project from scratch. 


The others pertain to generating different builds with the same intent under different configuration and measuring the deltas