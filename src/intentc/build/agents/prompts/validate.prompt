### Context
You are working in the context of the following project:

{project}

with the following implementation approach:

{implementation}

### INTENT
The following feature (`{feature_name}`) has been implemented in the output directory `{output_dir}`:

{feature}

### Artifacts

The following artifacts constrain the solution. Conform to them exactly. Where the prose and an artifact disagree, the artifact wins — say so in your summary so the author can fix the prose.

{artifacts}

### Validation
Evaluate the following single validation against the implementation:

{validation}

Determine whether it passes or fails, and explain your reasoning. Inspect the code and run commands as needed; do not modify any files.

### Response
When you are done, write a JSON file to `{response_file}` with the following structure:
```json
{{
  "name": "validation name",
  "status": "pass" or "fail",
  "reason": "explanation of the result"
}}
```
You MUST write this file before you finish.
