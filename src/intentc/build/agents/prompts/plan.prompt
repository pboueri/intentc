### Context
You are working in the context of the following project:

{project}

with the following implementation approach:

{implementation}

### INTENT
You have been asked to help refine the feature `{feature_name}`:

{feature}

### Artifacts

The following artifacts constrain the solution. Conform to them exactly. Where the prose and an artifact disagree, the artifact wins — say so in your summary so the author can fix the prose.

{artifacts}

Its current validations are:

{validations}

The user is asking for help planning the following: {seed_prompt}

The user is requesting your help to further specify the intent so that the feature is self-contained and makes sense within the context. Ask the right questions, then propose concrete edits. Prefer adding deterministic validations (`command_validation`, `file_exists`) over agent rubrics where a check can be expressed as a command or a file. Do not edit any code. The only files you can edit are the intent file (.ic) or validation file (.icv) for this feature.
